{% extends 'base.html.twig' %}

{% block title %}Hello SessionController!{% endblock %}

{% block body %}

<div>
    <h1>Hello {{ controller_name }}! ✅</h1>

    Test Liste Sessions:
    <ul>
        {% for row in sessions %}
            <li><a href="{{ path('show_session', {'id' : row.Id}) }}"> {{row}}  </a></li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
